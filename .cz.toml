[tool.commitizen]
name = "cz_conventional_commits"
version = "0.4.0"
tag_format = "v$version"
version_scheme = "semver"
update_changelog_on_bump = true
major_version_zero = true
# Keep kai.toml's version in lockstep with the tag on every `cz bump`.
version_files = [
    "kai.toml:version",
]
